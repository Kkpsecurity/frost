import React from 'react';

// Minimal test component
const TestComponent: React.FC = () => {
    return (
        <div>
            <h1>Test Component Works</h1>
            <p>This is a minimal React component for testing preamble detection</p>
        </div>
    );
};

export default TestComponent;
