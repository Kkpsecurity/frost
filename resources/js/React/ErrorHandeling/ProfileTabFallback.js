import { Alert } from "bootstrap";
import React from "react";

const ProfileTabFallback = () => {
    return <Alert variant={"danger"}>Error Loading ProfileTabs</Alert>;
};

export default ProfileTabFallback;
