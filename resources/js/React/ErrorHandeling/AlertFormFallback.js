import React from "react";
import { Alert } from "react-bootstrap";

export const AlertFormFallback = () => {
    return <Alert variant="danger">The Form failed to load:</Alert>;

    // Do some error log
};
