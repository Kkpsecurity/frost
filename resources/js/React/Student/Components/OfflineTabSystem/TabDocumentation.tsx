import React from "react";

const TabDocumentation = ({ lessons, ...props }) => (
    <div className="documentation-tab">
        {/* ...moved content from documentation tab... */}
        {/* Use lessons and other props as needed */}
    </div>
);

export default TabDocumentation;
